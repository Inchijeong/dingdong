<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dd.mappers.TokenMapper">

	<insert id="create">
		UPDATE tb_user
		SET bell_token=#{bellToken}
		WHERE user_no=#{userNo};
	</insert>

</mapper>